
<% if flash[:errors] %>
<% flash[:errors].each do |error| %>
    <p><%= error %></p>
    <% end %>
    <% end %>

<%= form_with model: @comment do |f| %>
	<%= f.label :description %><br />
	<%= f.text_field :description %><br />
    <%= f.submit %><br>
    <%= button_to "Profile Page", user_path(@user), method: :get %> 

<% end %>
